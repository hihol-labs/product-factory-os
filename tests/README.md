# Tests

This directory contains methodology fixtures and validation helpers.

Run:

```bash
python3 scripts/validate_structure.py
```

Fixtures are intentionally small. They prove that routing expectations, required outputs, and docs stay synchronized as the methodology evolves.

