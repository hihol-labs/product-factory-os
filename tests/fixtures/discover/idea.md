# Fixture: Discover

User request:

```text
Validate this product idea, identify target users, and compare alternatives before we plan the build.
```

Expected route:

```text
/project -> /discover
```
