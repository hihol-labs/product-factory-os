# Notes

This fixture checks that security audit remains source-read-only, uses `docs/rubrics/security.md`, and follows security-audit v2 with separate threat model, discovery, validation, attack path, coverage artifacts, and validated final report.
