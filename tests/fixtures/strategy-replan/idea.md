# Fixture: Strategy Replan

User request:

```text
Replan the roadmap, compare strategic options, and decide whether to pivot before the next release.
```

Expected route:

```text
/task -> /strategy
```
